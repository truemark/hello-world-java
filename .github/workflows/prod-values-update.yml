name: Values Only Update - Prod
on:
  workflow_dispatch:
  push:
    paths:
      - 'kubernetes/prod/**'

permissions:
  id-token: write
  contents: write

jobs:
  values-update-stage:
    name: Update Values in Deployment Repo
    uses: truemark/github-workflows/.github/workflows/prod-values-update.yml@argocd-demo
    secrets:
      app_id: ${{ secrets.RELEASE_BOT_ID }}
      private_key: ${{ secrets.RELEASE_BOT_KEY }}

deployment:
  image:
    tag: "61.31.5"
istio:
  virtualServices:
    enabled: true
    spec:
      default-route:
        hosts:
          - hello.truemark.io
        gateways:
          - istio-ingress/external
        http:
          - name: hello-world
            route:
              - destination:
                  host: hello-world
                  port:
                    number: 80